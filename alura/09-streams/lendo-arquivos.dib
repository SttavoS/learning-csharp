#!csharp

using System.IO;

var stream = new FileStream("contas.txt", FileMode.Open);
var buffer = new byte[1024];

stream.Read(buffer, 0, 1024);
var enconding = Encoding.UTF8;
var texto = enconding.GetString(buffer);

Console.Write(texto);
